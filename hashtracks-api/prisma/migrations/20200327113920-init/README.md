# Migration `20200327113920-init`

This migration has been generated by its-kyle-yo at 3/27/2020, 11:39:20 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200327113920-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,63 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id              String       @id @default(cuid())
+  twitterUserID   String       @unique
+  twitterHandle   String
+  isSubscribed    Boolean      @default(false)
+  isNewUser       Boolean      @default(true)
+  profileImageUrl String
+  name            String
+  commitments     Commitment[]
+  posts           Post[]
+  createdAt       DateTime     @default(now())
+}
+
+model Commitment {
+  id           String @id @default(cuid())
+  text         String
+  type         String
+  duration     Int
+  durationType String
+  author       User
+}
+
+model Post {
+  id                String   @id @default(cuid())
+  twitterPostID     String   @unique
+  createdAt         String
+  deconstructedDate String
+  text              String
+  source            String
+  hashtags          String[]
+  media             Media[]
+  author            User
+}
+
+model Media {
+  id             String  @id @default(cuid())
+  post           Post
+  twitterMediaID String  @unique
+  imageUrl       String?
+  videoUrl       String?
+  type           String
+}
+
+model Webhook {
+  id               String  @id @default(cuid())
+  twitterWebhookID String  @unique
+  url              String
+  valid            Boolean
+  createdAt        String
+  environment      String
+}
```


